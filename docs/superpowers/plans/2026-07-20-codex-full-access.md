# Codex 完全权限模式实施计划

1. 为配置默认值、新会话参数和恢复会话参数补充失败测试。
2. 将配置解析默认值切换为完全权限和实时搜索。
3. 调整 runner，使 bypass 和实时搜索配置覆盖同时作用于新会话与恢复会话。
4. 更新示例配置、CLI 初始化模板、README 和当前项目配置。
5. 运行定向测试、全量测试、类型检查和构建。
6. 重启 `codex-gateway`，执行真实网络访问并检查服务状态。
